
const settings = {
  //threshold for COVID alerts determined by cases/population
  covidAlertRatio: .05,
  //threshold for fire alert level
  fireAlertLevel: 3,
  alertsEnabled: true,
};

export default settings;
